// Mock PNG content - this would be an actual PNG image in a real implementation
// This is just a text placeholder since we can't generate binary image content directly
// In a real application, you would replace this with actual PNG files